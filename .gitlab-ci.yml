stages:
  - lint
  - test

variables:
  GIT_SUBMODULE_STRATEGY: recursive
  GIT_SUBMODULE_DEPTH: 20   # optional, keeps submodule fetch shallow to match GIT_DEPTH

lint:
  stage: lint
  image: python:3.9
  script:
    - source setup.sh ""
    - ./tests/run_linting
  tags:
    - cvmfs

analysis:
  stage: test
  script:
    - git submodule update --init --recursive modules/columnflow
    - source setup.sh dev
    - bash tests/run_analysis
  tags:
    - cvmfs
  only:
    - merge_requests
    - main
    - triggers
    - web
